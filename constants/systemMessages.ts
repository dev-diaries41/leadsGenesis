const noLeadsMessage = 'No stored leads. To get started head over to home screen to `Find Leads`.'
const noSummaryReportMessage = 'You currently do not have a summary report to view. To get started head over to home screen and press `Find Companies`.'
const getLeadDescription = 'Find companies in any postcode, with optional keyword filtering.';

export {noLeadsMessage, noSummaryReportMessage,getLeadDescription}